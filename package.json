{
  "name": "herdr-tab-titles",
  "private": true,
  "description": "Herdr plugin that names panes and tabs after the first coding-agent prompt.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/kewah/herdr-tab-titles.git"
  },
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "eval:titles": "npx --yes promptfoo@0.122.2 eval --config eval/promptfooconfig.yaml"
  },
  "engines": {
    "node": ">=22.22.0"
  }
}
